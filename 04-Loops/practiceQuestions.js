// // Q1. Calculate sum of first 'n' numbers.
// let n = Number(prompt("Enter a number:"));
// let sum = 0;
// for (let i = 1; i <= n; i++) {
//   sum += i;
// }
// console.log("Sum = ", sum);

// Q2. Calculate sum from "m" to "n"
let m = Number(prompt("Enter a starting number:"));
let n = Number(prompt("Enter a ending number:"));
let sum = 0;
for (let i = m; i <= n; i++) {
  sum += i;
}
console.log("Sum = ", sum);
